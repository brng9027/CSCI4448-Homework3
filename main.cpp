#include <iostream>
#include "Customer.h"
#include "store.h"
#include "tools.h"
#include <vector>
#include <string>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
using namespace std;

int main(){
  // std::vector<Customer> vec_of_customers;
  // for(int i = 0; i < 10; i++){
  //   Customer new_customer("rew" + to_string(i));
  //   vec_of_customers.push_back(new_customer);
  // }
  // for(int night = 1; night <= 35; night++){
  //   int num_of_customers_to_visit = rand() % 10 + 0;
  //   for(int custom_int = 0; custom_int < num_of_customers_to_visit; custom_int++){
  //
  //   }
  // }
  return 0;
}
